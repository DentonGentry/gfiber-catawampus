#!/bin/sh

if [ "$1" = "-r" ] && [ "$2" = "1ST_SERIAL_NUMBER" ]; then
  echo "1ST_SERIAL_NUMBER=123456789"
fi

if [ "$1" = "-r" ] && [ "$2" = "PLATFORM_NAME" ]; then
  echo "PLATFORM_NAME=ModelName"
fi

if [ "$1" = "-r" ] && [ "$2" = "GPN" ]; then
  echo "GPN=PartNumber"
fi

if [ "$1" = "-r" ] && [ "$2" = "HW_VER" ]; then
  echo "HW_VER=ver"
fi

exit 0
