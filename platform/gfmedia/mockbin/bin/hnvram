#!/bin/sh

if [ "$1" = "-r" ] && [ "$2" = "1ST_SERIAL_NUMBER" ]; then
  echo "1ST_SERIAL_NUMBER=000000001"
fi

if [ "$1" = "-r" ] && [ "$2" = "PLATFORM_NAME" ]; then
  echo "PLATFORM_NAME=FakeGFMedia"
fi

exit 0
